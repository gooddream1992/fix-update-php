
YUI.add("bolt-countdown",function(Y){var $=Y.get,$j=Y.JSON;BLT.Class.Countdown=function(args){this.init(args);}
BLT.Class.Countdown.prototype={store:{},init:function(args){this.id='countdown-'+args.id;if(Y.one('#'+this.id+' .days')){this.days=parseInt(Y.one('.days').get('innerHTML'));}else{this.days=0;}
this.hours=parseInt(Y.one('#'+this.id+' .hours').get('innerHTML'));this.mins=parseInt(Y.one('#'+this.id+' .mins').get('innerHTML'));this.secs=parseInt(Y.one('#'+this.id+' .secs').get('innerHTML'));this.endtime=args.endtime;var dthen=this.endtime*1000;var dnow=new Date();var ddiff=new Date(dthen-dnow);this.gsecs=Math.floor(ddiff.valueOf()/1000);this.countBack();this.publish('countdown:init');this.fire('countdown',this);},countBack:function(){if(this.gsecs<0){return;}
this.days=this.calculateAge(86400,100000);this.hours=this.calculateAge(3600,24);this.mins=this.calculateAge(60,60);this.secs=this.calculateAge(1,60);this.updateLabels();this.gsecs=this.gsecs-1;var q=new Y.AsyncQueue();q.add({fn:this.countBack,timeout:1000,context:this});q.run();},calculateAge:function(num1,num2){var s=((Math.floor(this.gsecs/num1))%num2).toString();return s;},updateLabels:function(){if(Y.one('#'+this.id+' .days')){Y.one('#'+this.id+' .days').set('innerHTML',this.days+' days');}
Y.one('#'+this.id+' .hours').set('innerHTML',this.hours+' hours');Y.one('#'+this.id+' .mins').set('innerHTML',this.mins+' mins');Y.one('#'+this.id+' .secs').set('innerHTML',this.secs+' secs');}}
Y.augment(BLT.Class.Countdown,Y.EventTarget);});